# Certificate photostats (The Loneliest People)

Source scans for *The File* (`build/chapters/ch-50.md`) and *What Became of Him*
(`build/chapters/ch-51.md`). Staged 2026-07-31 from `~/Pictures/Skool1*.png`,
`~/Pictures/skool2*.png`, and the adult *werk* credential scans.

These are the author's real school and early-career record. The prose dossiers ground
themselves in them. Embedded backmatter lives in `build/chapters/ch-52.md` (**The Photostats**),
which points at the print-sized JPEGs under `print/` (and yearbook plates under
`../yearbook/print/`). Keep the full PNG sources here; regenerate print embeds by
resampling to max edge ~1400px JPEG q80 before a release render.

Faces in yearbook group photos remain subject to the existing redaction pipeline under
`../yearbook/` before any public HTML/PDF embed.

Do not commit full ID numbers into rendered book *text* (ch-50 redacts the trailing digits).
